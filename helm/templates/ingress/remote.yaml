apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: open-balena-remote-http
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/ingress.class: open-balena-admin-haproxy
    cert-manager.io/issuer: open-balena-admin-certificate-issuer
spec:
  tls:
  - hosts:
    - remote.{{ .Values.global.hostname }}
    secretName: open-balena-remote-tls
  rules:
  - host: remote.{{ .Values.global.hostname }}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: remote
            port:
              number: 443